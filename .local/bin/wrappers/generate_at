#!/bin/sh

out="$(env $(grep -E '^(pp_key|pp_secret)=' "$XDG_DATA_HOME/secrets/priv") run_py genat.py "pycryptodome")"
val=$(printf '%s\n' "$out" | tail -n1)

printf %s "$val" | xclip -selection clipboard
printf '%s\n' "$val"
