#!/bin/sh

case $1 in
	"p" | "profiler") /opt/roku-bsprof.AppImage ;;
	"r" | "resource") /opt/roku-resource-monitor.AppImage ;;
	"s" | "stream") /opt/roku-stream-tester.AppImage ;;
	"d" | "deeplink") /opt/roku-deep-linking-tester.AppImage ;;
	*) /opt/roku-advanced-layout-editor.AppImage ;;
esac
