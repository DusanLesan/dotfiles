#!/bin/sh

. super_secret_file

case "$1" in
	"-q")
		nmcli connection down B
	;;
	*)
		nmcli device wifi hotspot con-name B ssid B band bg password "$hotspot_pass"
		[ $(ps -o stat= -p $$) = "S+" ] || notify-send "Wifi hotspot" "Wifi hotspot activated"
	;;
esac
