#!/bin/sh

clock=$(date '+%I')

# case "$clock" in
# 	"00") icon="🕛" ;;
# 	"01") icon="🕐" ;;
# 	"02") icon="🕑" ;;
# 	"03") icon="🕒" ;;
# 	"04") icon="🕓" ;;
# 	"05") icon="🕔" ;;
# 	"06") icon="🕕" ;;
# 	"07") icon="🕖" ;;
# 	"08") icon="🕗" ;;
# 	"09") icon="🕘" ;;
# 	"10") icon="🕙" ;;
# 	"11") icon="🕚" ;;
# 	"12") icon="🕛" ;;
# esac

three="cal -m --color=always"

case $BLOCK_BUTTON in
	1) notify-send "date" "$(cal -m --color=always | sed "s/..7m/<b><span color=\"red\">/;s/..27m/<\/span><\/b>/")" ;;
	3) notify-send "date" \
		"$(cal $(date -d "$(date +%Y-%m-15) -1 month" +"%B %Y") \
		&& cal -m --color=always | sed "s/..7m/<b><span color=\"red\">/;s/..27m/<\/span><\/b>/"\
		&& cal $(date -d "$(date +%Y-%m-15) +1 month" +"%B %Y") \
		&& printf "<b><span color=\"red\">$(TZ=America/Los_Angeles date "+%a, %b %d, %H:%M")<\/span><\/b>" )"
 ;;
	
esac

# date "+%Y %b %d (%a) $icon%I:%M%p"
date "+%a, %b %d, %H:%M"
