#!/bin/sh

WID=$(xdotool selectwindow)

cat ~/.local/share/team | while read -r user; do
	xdotool windowactivate $WID type --delay 20 "$user"
	sleep 0.3
	xdotool key KP_Enter
	xdotool key ctrl+a BackSpace
done
